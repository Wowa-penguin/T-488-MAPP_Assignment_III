import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 8,
        gap: 10,
    },
    poster: {
        width: 150,
        height: 200,
    },
    genresContainer: {
        flexDirection: 'row',
        justifyContent: 'space-evenly',
    },
});
