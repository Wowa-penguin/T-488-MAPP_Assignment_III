import globalStyles from '@/styles/globalStyles';
import { Text, View } from 'react-native';

const Showtime = () => {
    return (
        <View>
            <Text style={globalStyles.defaultTextColor}>Showtiem</Text>
        </View>
    );
};

export default Showtime;
